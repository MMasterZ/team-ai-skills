# ป้องกันไม่ให้ Token หลุดขึ้น GitHub (สำคัญที่สุด)
.npmrc

# ไม่เอาโฟลเดอร์แพ็กเกจที่โหลดมาขึ้น Git
node_modules/

# ไฟล์ขยะของระบบปฏิบัติการหรือ Editor (ใส่เผื่อไว้)
.DS_Store
Thumbs.db
.idea/
.vscode/